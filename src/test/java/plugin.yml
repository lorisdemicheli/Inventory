main: ${project.class.main}
name: ${project.name}
version: ${project.version}
author: ${project.dev.name}
description: ${project.description}
api-version: 1.15
commands:
  test:
    description: Open the test inventory.
    usage: /test
